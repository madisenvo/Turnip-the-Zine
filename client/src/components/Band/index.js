import React from "react";
import styled from "styled-components";

export default function Band() {
    return (
        <div className="band">
            <h1>FEATURED BAND</h1>
        </div>
    );
}
